<div class="card" style="background-color: unset">
  <div class="card-header" id="<%= card_button_id %>">
    <h1 class="mb-0">
      <button class="btn btn-link collapsed accordion-btn-text" data-toggle="collapse" data-target="#<%= card_content_id %>" aria-expanded="false" aria-controls="<%= card_content_id %>">
        <%= report_title %>
      </button>
    </h1>
  </div>

  <div id="<%= card_content_id %>" class="collapse" aria-labelledby="<%= card_button_id %>" data-parent="#accordion">
    <div class="card-body">

      <div class="container">
        <div class="row justify-content-between">
          <div class="col-8">
            <div class="container nav-container no-horiz-padding">
              <nav class="nav-justified">
                <div class="nav nav-tabs" id="nav-tab" role="tablist">
                  <%= nav_tab(name + '_text', 'Text', true) %>
                  <%= nav_tab(name + '_json', 'JSON') %>
                  <%= nav_tab(name + '_yaml', 'YAML') %>
                </div>
              </nav>
            </div>
          </div>
          <div class="col-4 align-self-center" style="text-align: right;">
            <button id="<%= copy_button_id %>" class="rpt-copy-button">Copy</button>
          </div>
        </div>
      </div>

      <br/>
      <br/>

      <div class="container no-horiz-padding">
        <div class="tab-content" id="<%= content_tab_id %>">
          <%= nav_content(name: name + '_text', html_text: report_text, active: true) %>
          <%= nav_content(name: name + '_json', html_text: report_json) %>
          <%= nav_content(name: name + '_yaml', html_text: report_yaml) %>
        </div>
      </div>
    </div>
  </div>
</div>
