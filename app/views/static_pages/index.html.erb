<div class="container-fluid no-horiz-padding">

<!--  <script> navigator.clipboard.writeText("Hello from Rails").then(() => { alert("Wrote to clipboard."); }) </script>-->
  <h3 class="title">The Songwriting Team of</h3>
  <div class="responsive-xs-only"><h1 class="title">Tepper & Bennett</h1></div>
  <div class="responsive-no-xs"><h1 class="title">Sid Tepper and Roy C. Bennett</h1></div>
  <div><h3 class="title">Esteemed Hitmakers of the 40's, 50's, and 60's</h3></div>
  <br/>

  <div class="container nav-container no-horiz-padding">
    <nav class="nav-justified">
        <div class="nav nav-tabs" id="nav-tab" role="tablist" style="align: center">
          <%= nav_tab('home', 'Home', true) %>
          <%= nav_tab('songs', 'Songs') %>
          <%= nav_tab('genres', 'Genres') %>
          <%= nav_tab('elvis', 'Elvis') %>
          <%= nav_tab('resources', 'Resources') %>
          <% if @include_reports %>
            <%= nav_tab('reports', 'Reports') %>
          <% end %>
          <%= nav_tab('inquiries', 'Inquiries') %>
        </div>
    </nav>
  </div>

  <br/>
  <br/>

  <div class="container no-horiz-padding">
    <div class="tab-content" id="nav-tabContent">
      <%= nav_content(name: 'home', active: true) %>
      <%= nav_content(name: 'songs') %>
      <%= nav_content(name: 'genres') %>
      <%= nav_content(name: 'elvis') %>
      <%= nav_content(name: 'resources') %>
      <% if @include_reports %>
        <%= nav_content(name: 'reports') %>
      <% end %>
      <%= nav_content(name: 'inquiries') %>
    </div>
  </div>

  <%= render 'youtube-player-modal' %>

</div>
